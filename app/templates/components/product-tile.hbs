<div class="col s6 l3">
  <div class="card small">
    <div class="card-image">
      {{#link-to 'product-page' product.id}}<img class="responsive-img" src='{{product.image}}'>{{/link-to}}
    </div>
    <div class="card-content white-text">
      <p class="product-text">{{product.name}}</p>

    </div>
    <div class="card-action">
      <p class="price">${{product.price}}</p>
      <button class="price-button btn waves-effect waves-light" {{action 'addToCart' product}}>Add to Cart</button>
    </div>
  </div>
</div>
