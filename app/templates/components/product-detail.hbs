<div>

    <img src='{{product.image}}'>
    <h2>{{product.name}}</h2>
    <h3>${{product.price}}</h3>
    <h3>{{product.description}}</h3>
    <h3>{{product.category}}</h3>
    <h3>Number in stock: {{product.stock}}</h3>
    
</div>
